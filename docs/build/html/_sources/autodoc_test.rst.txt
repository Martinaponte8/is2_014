def add_one(number):

    """Add one to a number

    This function will take a number, any number and add one to it.
    That's it, not very exciting

    :param number: A number, that we will add one to
    :type number: int
    :return: A number that is one more than the number provided
    :rtype: int
    """
    return number + 1